"""Axe 3 — Cohérence graphe de dépendances ↔ maquette."""


def check_dep_graph_vs_maquette(data: dict) -> list[dict]:
    """Axe 3a: All codes in the dependency graph must exist in the maquette."""
    return []


def check_maquette_dep_graph_coverage(data: dict) -> list[dict]:
    """Axe 3b: All maquette modules should appear in the dependency graph."""
    return []
